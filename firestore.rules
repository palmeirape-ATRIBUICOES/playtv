rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all collections (campaigns and playlists) for CRM sync and client portal
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
