rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to the campaigns collection (single user app)
    match /campaigns/{document=**} {
      allow read, write: if true;
    }
  }
}
